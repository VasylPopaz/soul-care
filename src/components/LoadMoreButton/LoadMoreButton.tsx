export const LoadMoreButton = ({ ...rest }) => {
  return (
    <button
      type="button"
      className="btn-primary block mx-auto py-[14px] px-12"
      {...rest}
    >
      Load more
    </button>
  );
};
